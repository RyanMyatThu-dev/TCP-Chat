# Learning Log

Use one entry per study session. Be specific: "understood sockets" is weak;
"explained why `accept()` returns a second socket and demonstrated it with two
clients" is evidence.

## Entry template

### YYYY-MM-DD - topic

- Goal:
- Prediction before coding:
- What I implemented:
- Error or surprising behavior:
- Root cause in my own words:
- Evidence that it works:
- Vim command learned:
- Question still open:
- Next smallest step:

## 2026-09-22 - project reset

- Goal: Rebuild the TCP chat application from understanding rather than copying.
- What I implemented: Pending - Milestone A.
- Evidence that it works: Pending.
- Next smallest step: Create the server socket and explain `AF_INET` and
  `SOCK_STREAM` before adding `bind()`.
